/* this is a comment */
/* a multi-
		  line comment */
/* a /* weird comment */
// one-line comment
var string = /* comment */'/* comment in string */';

move_to_the_same_line()

				dont_move();
				
if ( / a + bfoo.* /i.test( bar )  && ( a / b ) * c + d - e) { /* blah */
	statement();
	statement()
}