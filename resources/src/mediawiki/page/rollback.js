/*!
 * Enhance rollback links by using asynchronous API requests,
 * rather than navigating to an action page.
 *
 * @since 1.27
 * @author Timo Tijhof
 */
( function ( mw, $ ) {

	$( function () {
	} );

}( mediaWiki, jQuery ) );
