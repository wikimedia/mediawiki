( function ( mw ) {
	/**
	 * Supported highlight colors.
	 * Warning: These are also hardcoded in "styles/mw.rcfilters.variables.less"
	 *
	 * @type {[string]}
	 */
	mw.rcfilters.HighlightColors = [ 'blue', 'green', 'yellow', 'orange', 'red' ];
}( mediaWiki ) );
